"""
services/ — Business logic layer.

Route handlers in main.py should stay thin (parse request → call service → return response).
All heavy lifting (database queries, validation, external API calls) goes here
in clearly named service functions.

This package is empty in Phase 1. We'll add modules like:
  - meeting_service.py   (create/join/end meetings)
  - chat_service.py      (send/fetch chat messages)
  - recording_service.py (manage recording metadata)
"""
