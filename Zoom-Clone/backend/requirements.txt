# =============================================================================
# Zoom Clone Backend — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
# =============================================================================

# --- Web Framework ---
fastapi>=0.115.0          # Async web framework for building APIs
uvicorn[standard]>=0.32.0 # ASGI server to run FastAPI

# --- Database ---
sqlalchemy[asyncio]>=2.0   # ORM with async support
aiosqlite>=0.20.0          # Async SQLite driver (used by SQLAlchemy)

# --- Data Validation ---
pydantic>=2.0              # Request/response schema validation (used by FastAPI)

# --- Utilities ---
python-dateutil>=2.9.0     # Flexible date/time parsing for seed data
